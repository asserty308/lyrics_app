name: lyrics
description: An app to search for lyrics.

version: 0.0.5+5

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  curved_navigation_bar:
  
  flutter_core:
    git:
      url: git://github.com/asserty308/flutter_core.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
