name: lyrics
description: An app to search for lyrics.

version: 0.0.1+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  
  flutter_core:
    path: '../flutter_core/'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
